version: 0.2

phases:
  build:
    commands:
      - aws s3 sync . s3://datai.tec.br --delete --exclude ".git/*" --exclude "buildspec.yml" --exclude "README.md"
  post_build:
    commands:
      - aws cloudfront create-invalidation --distribution-id E7UM7BW7AAB7B --paths "/*"
